def grade(data):
    if(data["score"] != 1):
        data["feedback"]["q1"] = "The runtime for the algorithm we're using is in terms of $|V|$ and $|E|$. What do you know about the number of edges in relation to the number of vertices for this graph?"

